# HTTP 响应码

## 2XX

## 3XX 重定向

- 301 **永久**重定向

请求的资源被**彻底、永久**迁移到新 URL，原地址废弃，以后所有的请求都应该直接访问新地址，不再访问旧地址。

浏览器缓存默认**强缓存**，下次直接访问新地址。

> 用户访问 a.com → 301 → b.com，之后用户再输 a.com，浏览器直接去 b.com，不发请求给 a.com。

- 302 **临时**重定向

请求的资源**暂时**放在新 URL，原地址还保留有效，未来可能恢复使用，客户端下次仍应请求原地址。

浏览器**默认不缓存**，每次请求都会先请求旧地址。