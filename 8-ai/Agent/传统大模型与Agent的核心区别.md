# 传统大模型与AI Agent的核心区别

智能回答（传统大模型）本质上是“静态响应系统”，其核心行为是基于输入提示（prompt）进行概率化文本生成，不具备自主目标管理、环境交互、工具调用和多步推理闭环能力；而 AI Agent 是“动态决策主体”，具备感知（Perceive）、规划（Plan）、行动（Act）、反思（Reflect）的完整认知循环，能主动分解目标、选择并调用外部工具（如API、数据库、计算器）、观察执行结果、修正策略，最终达成用户隐含或显式的长期目标。