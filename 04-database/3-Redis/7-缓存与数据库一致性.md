---
tags:
  - Redis
  - 缓存
category: 数据库/Redis
---

标准方案：先更新数据库，再删除缓存。

1）先删缓存，再更新 DB（有问题！）
请求 A：删缓存 → 更新 DB请求 B：查缓存不存在 → 读旧 DB → 把旧数据写回缓存→ 缓存是旧的，DB 是新的，长期不一致！
结论：绝对不能用。
2）先更新 DB，再删缓存（正确方案）
极大概率一致
极端场景才会短暂不一致（秒级）
业务可接受

## 一句话总结

> 推荐先更新DB再删缓存方案，绝不能用先删缓存再更新DB（旁路缓存模式）。
