# 数组与链表的区别

> 数组连续内存、随机访问快、增删慢；链表不连续、查找慢、增删快、大小灵活。

- 内存布局

数组：内存连续

链表：内存不连续

- 访问方式

数组支持通过索引下标随机访问，复杂度 O(1)

链表不支持随机访问，只能从头遍历，查找复杂度 O(n)

- 插入 / 删除

数组：需要移动元素，O(n)

链表：只需改指针，O(1)

- 大小

数组：固定大小，扩容麻烦

链表：动态大小，随时增删

- 适用场景

数组：频繁查询、少改动

链表：频繁增删、少查询