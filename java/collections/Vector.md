# Vector

## 数据结构

- 数组（初始容量默认 10）
- 动态扩容（默认 2 倍）
- 有序存储
- 支持存储`null`。

## 特点

- **线程安全** 

所有**公共方法**都使用`synchronized`修饰，保证多线程并发访问时的数据一致性。

线程安全仅针对**单个方法**（方法级锁），**复合操作**仍可能出现并发问题，需手动加锁。

任何需要“先检查后操作”或者“读取-修改-写入”的模式，都可能存在并发风险。

- 性能较低

锁额外开销，**单线程**环境下可以用`ArrayList`替代。
