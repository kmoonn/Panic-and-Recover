---
tags:
  - MySQL
  - 索引
category: 数据库/MySQL/索引
---

① 聚簇索引（主键索引）
就是 B+ 树的叶子节点直接存整行数据
每张表 有且只有一个
默认是主键
查主键：直接定位整行，最快
② 二级索引（普通索引）
叶子节点只存 索引值 + 主键
查到后，要拿主键回表去聚簇索引查完整数据
回表 = 多一次 IO，比聚簇索引慢

## 一句话总结

> 聚簇索引叶子存整行数据查主键最快，二级索引叶子只存索引值+主键需回表多一次I/O。
