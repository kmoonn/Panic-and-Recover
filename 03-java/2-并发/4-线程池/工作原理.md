---
tags:
  - Java
  - 并发
  - 线程池
category: Java/并发/线程池
---

# 线程池工作原理

任务提交后，线程池按以下顺序处理：

1. **核心线程未满** → 新建核心线程执行
2. **核心线程已满、队列未满** → 任务放入阻塞队列
3. **队列已满、未达最大线程数** → 新建非核心线程执行
4. **达到最大线程数** → 触发拒绝策略

## 一句话总结

> 任务先填核心线程，再入队列，队列满则创建非核心线程，达上限则触发拒绝策略