# 索引工作原理

MySQL 索引 = B+ 树
作用：把 “全表扫描” 变成 “树查找”，从 O (n) 变 O (log n)

1. 为什么不用哈希、二叉树、红黑树？
哈希：等值查询快，但不能范围查询（> < between）
普通二叉树：可能退化成链表，等于没用
红黑树：树太高，磁盘 IO 太多，慢
B+ 树：矮胖、多叉、磁盘 IO 少、支持范围查询，完美适配数据库

2. B+ 树到底长啥样？
只有叶子节点存真实数据
非叶子只存索引键 + 指针，用来指路
叶子节点之间用链表串起来
树很矮，一般 3～4 层就能存千万数据
查一条数据，只需要 3～4 次磁盘 IO，极快。