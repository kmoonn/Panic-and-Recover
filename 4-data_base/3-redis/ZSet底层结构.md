跳表时间复杂度、为什么有两种结构

为了省内存 + 高性能兼顾：
数据少 → ziplist 省内存
数据多 → skiplist 保证查询速度
Redis 自动切换，对用户透明。

查找 / 插入 / 删除：O (log n)
范围查询：O (log n + m)
媲美红黑树，但实现更简单、支持范围查询、并发更友好。

ziplist（压缩列表）
元素少、值小的时候用
内存紧凑、省空间
skiplist + hash（跳表 + 哈希表）
元素多了自动转
hash 做 O (1) 查找
skiplist 做排序、范围查询
ZSet = 跳表 (排序) + 哈希表 (查值)

